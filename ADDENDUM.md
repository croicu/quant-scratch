# Addendum

This repo (or a repo generated from it) originates from **[croicu/tpl-py](https://github.com/croicu/tpl-py)**.

This file is the index of template updates meant for downstream instances. See `CLAUDE.md`'s
`Template Sync` section for the read/write protocol. Each row below links to a full entry under
`addendum/`.

| Timestamp | Title | File |
|---|---|---|
